spring:
    datasource:
        url: jdbc:mysql://localhost:3306/licenseDB
        username: root
        password: root
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
        hibernate:
            ddl-auto: update
        show-sql: true
